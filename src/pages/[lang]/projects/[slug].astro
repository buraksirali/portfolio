---
import BaseLayout from '../../../components/smart/BaseLayout.astro';
import ProjectDetail from '../../../components/smart/ProjectDetail.astro';
import { appConfig } from '../../../config';
import { createTranslator, type Locale } from '../../../i18n';

const locale = (Astro.params.lang ?? appConfig.locales.defaultLocale) as Locale;
const slug = Astro.params.slug ?? '';
Astro.locals.locale = locale;
const translator = createTranslator(locale);
---

<BaseLayout
	title={`${translator('seo.projectTitle')}: ${slug}`}
	description={translator('projects.title')}
	urlPathname={`/${locale}/projects/${slug}/`}
>
	<ProjectDetail slug={slug} />
</BaseLayout>
