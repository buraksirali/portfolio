---
import { appConfig } from '../config';
import { defaultLocale } from '../i18n';

const targetLocale = defaultLocale ?? appConfig.locales.defaultLocale;
---

<meta http-equiv="refresh" content={`0; url=/${targetLocale}/`} />
