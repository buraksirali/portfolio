import type express from 'express';
import type { RouteDependencies } from './types';

// Placeholder for API routes; none are defined yet so this is a no-op.
export const registerApiRoutes = (_app: express.Express, _deps: RouteDependencies) => {
	// Intentionally empty to preserve current behavior.
};
